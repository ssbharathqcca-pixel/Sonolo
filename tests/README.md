# Sonolo Tests

Cross-cutting integration and end-to-end tests that span `apps/` and `backend/`.

Unit tests live next to the code they cover (in `apps/mobile`, `apps/web`, and `backend`). This folder is only for tests that need multiple services running at once. Empty for now — the first cross-service test arrives with the tasks that wire the services together.
