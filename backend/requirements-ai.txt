# Optional AI extras for real (non-mock) providers (SN-016).
# Install with: pip install -r requirements-ai.txt
# The backend runs fully on the Mock providers without these.
faster-whisper==1.1.1
edge-tts==6.1.19
